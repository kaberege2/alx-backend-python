apiVersion: v1
kind: Service
metadata:
  name: messaging-app-switch
  namespace: messaging-app
spec:
  selector:
    app: messaging-app
    variant: blue
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
